name: Trigger auto deployment for yo-containerapp5

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/yo-containerapp5-AutoDeployTrigger-ef880c37-213e-4d00-84c2-29e9d66f16c3.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to container registry
        uses: docker/login-action@v1
        with:
          registry: yoonaohacr.azurecr.io
          username: ${{ secrets.YOCONTAINERAPP5_REGISTRY_USERNAME }}
          password: ${{ secrets.YOCONTAINERAPP5_REGISTRY_PASSWORD }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: yoonaohacr.azurecr.io/yo-containerapp5:${{ github.sha }}
          file: ./Dockerfile
          context: ./


  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.YOCONTAINERAPP5_AZURE_CREDENTIALS }}


      - name: Deploy to containerapp
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az config set extension.use_dynamic_install=yes_without_prompt
            az containerapp registry set -n yo-containerapp5 -g DefaultResourceGroup-CUS --server yoonaohacr.azurecr.io --username  ${{ secrets.YOCONTAINERAPP5_REGISTRY_USERNAME }} --password ${{ secrets.YOCONTAINERAPP5_REGISTRY_PASSWORD }}
            az containerapp update -n yo-containerapp5 -g DefaultResourceGroup-CUS --image yoonaohacr.azurecr.io/yo-containerapp5:${{ github.sha }}
